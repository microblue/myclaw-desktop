# CLAUDE.md — Agent 启动上下文
# Claude Code 每次 session 自动加载此文件

## 启动必读

立即执行（不需要用户提示）：
1. 读 `AGENTS.md` — 你的身份、职责、启动指令
2. 如有 `SOUL.md` — 读，这是你的性格
3. 如有 `MEMORY.md` — 读，这是项目长期记忆
4. 如有 `TODO.md` — 读，了解待办任务

不用等用户说读一下xxx，直接读。
